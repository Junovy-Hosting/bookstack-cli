# Nested Page Structure Example

This demonstrates the new nested page structure where each page has its own folder containing:

- `page.md` (or `.html`/`.txt`) - The page content
- `.page-metadata.json` - Optional metadata with name and priority

This structure allows for:
- Better organization of complex pages
- Priority-based ordering
- Consistent import/export workflows
- Future expansion for page-specific assets